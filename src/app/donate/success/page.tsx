import { DonateSuccessContent } from "./success-content";

/** Legado: mesma UI que `/donation-success`. Prefira `DONATE_SUCCESS_URL` → `/donation-success`. */
export default function DonateSuccessPage() {
  return <DonateSuccessContent />;
}
